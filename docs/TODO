* Restrict type imports/exports (see Make.hs for details.)
* Come up with a simple test case that triggers the Firefox JS bug where
  variables that are bound in a closure under some circumstances get unbound.
* The codegen doesn't memoize local thunks; that's quite horrible, so we need
  to fix it.
